#include <graphics.h>
#include <conio.h>

int main() {
    int gd = DETECT, gm;
    initgraph(&gd, &gm, "");

    circle(250, 200, 100); // x=250,y=200,radius=100
    getch();
    closegraph();
    return 0;
}

/*
Output:
A circle of radius 100 is drawn at (250,200)
*/
